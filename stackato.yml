name: chat-mongo

framework:
  type: python

processes:
  web: python app.py

requirements:
  pypm:
    - tornado
    - pymongo
  pip:
    - pycurl

services:
  mongodb: mongo-chat

min_version:
  server: 0.9.0.143
